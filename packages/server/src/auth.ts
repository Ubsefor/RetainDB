// OSS: authentication is handled via RETAINDB_API_KEY env var (see middleware/auth.ts)
// This file is a stub to satisfy any residual imports.
export const auth = null;
export type Session = { user: { id: string } };
